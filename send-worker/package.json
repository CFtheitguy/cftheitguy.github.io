{
  "name": "linear-send",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + R2 backend for Linear Send at send.linearit.co (send several files at full size with one link that expires).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
