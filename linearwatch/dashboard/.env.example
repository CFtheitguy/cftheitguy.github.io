# Base URL of the LinearWatch Worker API.
# Local dev default (if unset) is http://127.0.0.1:8787 — see src/api.js.
# In production set this to your deployed Worker, e.g. https://linearwatch-api.<subdomain>.workers.dev
VITE_API_BASE=http://127.0.0.1:8787
