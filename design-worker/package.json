{
  "name": "design-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker for Linear Design (design-api.linearit.co): stock photo search via Pexels and the Magic Write copy assistant via the Anthropic API. Holds both API keys as encrypted secrets so they never reach the browser.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node test/run.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "wrangler": "^4.138.0"
  }
}
