{
  "type": "object",
  "properties": {
    "lockSalt": {
      "title": "PIN salt (hex)",
      "description": "Random salt for the provisioned PIN. Generated by tools/provision.mjs.",
      "type": "string"
    },
    "lockHash": {
      "title": "PIN hash (hex)",
      "description": "PBKDF2-SHA256 of the PIN. The PIN itself never appears in policy.",
      "type": "string"
    },
    "lockIterations": {
      "title": "PBKDF2 iterations",
      "type": "integer"
    },
    "enabled": { "title": "Filtering on", "type": "boolean" },
    "safeSearch": { "title": "Force SafeSearch and YouTube Restricted Mode", "type": "boolean" },
    "keywordsEnabled": { "title": "Scan page text", "type": "boolean" },
    "urlKeywordsEnabled": { "title": "Scan web addresses", "type": "boolean" },
    "guardSettingsPage": { "title": "Block the extensions page while locked", "type": "boolean" },
    "keywordThreshold": { "title": "Keyword sensitivity", "type": "integer" },
    "unlockMinutes": { "title": "Minutes before the settings relock", "type": "integer" },
    "categories": {
      "title": "Category switches",
      "type": "object",
      "properties": {
        "adult": { "type": "boolean" },
        "gambling": { "type": "boolean" },
        "social": { "type": "boolean" },
        "video": { "type": "boolean" },
        "games": { "type": "boolean" }
      }
    },
    "allowlist": {
      "title": "Always-allowed domains",
      "type": "array",
      "items": { "type": "string" }
    },
    "blocklist": {
      "title": "Always-blocked domains",
      "type": "array",
      "items": { "type": "string" }
    },
    "remoteLists": {
      "title": "Subscribed blocklist URLs",
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
